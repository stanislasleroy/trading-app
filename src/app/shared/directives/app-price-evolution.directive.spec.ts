import { AppPriceEvolutionDirective } from './app-price-evolution.directive';

describe('AppPriceEvolutionDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPriceEvolutionDirective();
    expect(directive).toBeTruthy();
  });
});
